{
  "name": "vue-client",
  "version": "1.0.0",
  "description": "Nuxt.js project",
  "author": "ah100101 <ah100101@gmail.com>",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "build:start": "npm run build && npm run start",
    "generate": "nuxt generate",
    "copy": "ncp ./dist ../firebase/public",
    "generate:copy": "npm run generate && npm run copy",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint"
  },
  "dependencies": {
    "ncp": "^2.0.0",
    "nuxt": "^1.0.0"
  },
  "devDependencies": {
    "babel-eslint": "^8.2.1",
    "eslint": "^4.15.0",
    "eslint-friendly-formatter": "^3.0.0",
    "eslint-loader": "^1.7.1",
    "eslint-plugin-vue": "^4.0.0",
    "node-sass": "^4.9.0",
    "pug": "^2.0.3",
    "pug-loader": "^2.4.0",
    "sass-loader": "^7.0.1",
    "workbox-webpack-plugin": "^3.1.0"
  }
}
